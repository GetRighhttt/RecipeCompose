package com.example.recipe_app_compose.features.location.domain.preferences

/** The user's saved choice for how nearby restaurant search starts. */
enum class LocationPreference {
    AskEveryTime,
    CurrentLocation,
}
